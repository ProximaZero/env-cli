import * as Crypto from 'crypto-js';
import * as prompts from 'prompts';
import * as fs from 'fs';
import * as dotenv from 'dotenv';

export class ZeroCli {
    async main() {

    }
}
(async () => new ZeroCli().main())().then();